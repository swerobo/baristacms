# BaristaCMS Docker Environment Configuration
# Copy this file to .env and configure your settings

# ============ MySQL Database ============
MYSQL_ROOT_PASSWORD=baristacms2024
MYSQL_DATABASE=baristacms
MYSQL_USER=baristacms
MYSQL_PASSWORD=baristacms2024
MYSQL_PORT=3306

# ============ Ports ============
BACKEND_PORT=3001
FRONTEND_PORT=80

# ============ API URL ============
# URL where the backend API will be accessible from the browser
# For local development: http://localhost:3001
# For production: https://api.yourdomain.com
VITE_API_URL=http://localhost:3001

# ============ Azure AD Authentication ============
# Required for M365 authentication
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
VITE_AZURE_CLIENT_ID=your-client-id
VITE_AZURE_TENANT_ID=your-tenant-id

# ============ Email Configuration (Optional) ============
# Provider: 'graph' for Microsoft Graph API, 'smtp' for SMTP
EMAIL_PROVIDER=graph
EMAIL_AZURE_TENANT_ID=your-tenant-id
EMAIL_AZURE_CLIENT_ID=your-client-id
EMAIL_AZURE_CLIENT_SECRET=your-client-secret
EMAIL_FROM=noreply@yourdomain.com

# ============ Development Settings ============
# Set to true to bypass authentication (development only!)
AUTH_BYPASS=false
AUTH_BYPASS_EMAIL=admin@example.com
AUTH_BYPASS_NAME=Admin User
