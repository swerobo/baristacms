# Database Configuration
# =====================

# Database type: 'sqlite' (default), 'azure', or 'mysql'
DB_TYPE=sqlite

# SQLite settings (for local development)
DB_FILE=mydatabase.db

# Azure SQL settings
# Option 1: Connection string
# AZURE_SQL_CONNECTION_STRING=Server=tcp:your-server.database.windows.net,1433;Database=officetool;User Id=your-user;Password=your-password;Encrypt=true;

# Option 2: Individual settings
# AZURE_SQL_SERVER=your-server.database.windows.net
# AZURE_SQL_DATABASE=serverdb
# AZURE_SQL_USER=your-user
# AZURE_SQL_PASSWORD=your-password

# MySQL/MariaDB settings
# Option 1: Connection string
# MYSQL_CONNECTION_STRING=mysql://user:password@localhost:3306/serverdb

# Option 2: Individual settings
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_DATABASE=serverdb
# MYSQL_USER=root
# MYSQL_PASSWORD=your-password

# Server Configuration
# ====================
PORT=3001
NODE_ENV=development

# Authentication
# ==============
# Set to 'true' to bypass auth in development
AUTH_BYPASS=true

# Azure AD settings (optional, for production)
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id

# CORS
# ====
# WEB_APP_URL=https://your-app.azurewebsites.net
# MOBILE_APP_URL=your-mobile-app-url

# Email Configuration
# ===================
# Choose provider: 'graph' (recommended for Office 365) or 'smtp'
# EMAIL_PROVIDER=graph

# Option 1: Microsoft Graph API (OAuth2) - RECOMMENDED
# Requires Azure AD App Registration with Mail.Send permission
# EMAIL_PROVIDER=graph
# AZURE_TENANT_ID=your-tenant-id          (same as auth if already set)
# AZURE_CLIENT_ID=your-client-id          (same as auth if already set)
# AZURE_CLIENT_SECRET=your-client-secret  (create in Azure Portal)
# EMAIL_FROM=sender@company.com           (the mailbox to send from)

# Option 2: SMTP (legacy - may require SMTP AUTH enabled)
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=your-email@company.com
# SMTP_PASSWORD=your-password-or-app-password
# SMTP_FROM=your-email@company.com
